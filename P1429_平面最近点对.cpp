#include <bits/stdc++.h>
#define LL long long
using namespace std;
LL divide_and_conquer(const vector<pair<LL, LL>> &points, int l, int r) {
    
}
int main() {
    ios::sync_with_stdio(0);
    cin.tie(0);
    int n;
    cin >> n;
    vector<pair<LL, LL>> points(n);
    for (int i = 0; i < n; ++i) cin >> points[i].first >> points[i].second;
    sort(points.begin(), points.end());

}
